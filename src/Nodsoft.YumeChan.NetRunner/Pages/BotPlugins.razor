@page "/BotPlugins"
@attribute [Authorize]
@inject Nodsoft.YumeChan.Core.YumeCore BotService

@using Nodsoft.YumeChan.PluginBase;

<h1>Plugins</h1>
<br />
<h3>Currently Loaded : @BotService.CommandHandler.Plugins.Count Plugins</h3>
<br />

<table class="table">
    <thead>
        <tr>
            <th>Assembly</th>
            <th>Plugin Name</th>
            <th>Version</th>
            <th>Active</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Plugin pluginManifest in BotService.CommandHandler.Plugins)
        {
            <tr>
                <td>@pluginManifest.PluginAssemblyName</td>
                <td>@pluginManifest.PluginDisplayName</td>
                <td>@pluginManifest.PluginVersion</td>
                <td>@(pluginManifest.PluginLoaded ? "Yes" : "No")</td>
            </tr>
        }
    </tbody>
</table>